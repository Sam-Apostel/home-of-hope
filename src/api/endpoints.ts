const API_ENDPOINT = 'https://api.tigrr.be/'

export const newOrder = `${API_ENDPOINT}new/`;
export const orderStatus = `${API_ENDPOINT}order/open/`;
export const log = `${API_ENDPOINT}log/`;

export default {
	newOrder,
	orderStatus,
	log
};
